{
  "name": "fleet-management-backend",
  "version": "1.0.0",
  "description": "Fleet Management & Telematics Integration Platform - Backend",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=development node --watch src/server.js",
    "build": "echo \"No build step required\"",
    "postinstall": "prisma generate",
    "start": "NODE_ENV=production node src/server.js",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate deploy",
    "db:seed": "node prisma/seed.js"
  },
  "dependencies": {
    "@prisma/client": "^5.8.0",
    "axios": "^1.6.7",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.1.2",
    "node-cron": "^3.0.2"
  },
  "devDependencies": {
    "prisma": "^5.8.0"
  }
}
